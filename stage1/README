For training:
python train_frcnn.py -o simple -p input.txt

input.txt contains the training coordinates.

For testing:
python test_frcnn.py -p /path/to/test_images/

Result images in result_images folder

Result coordinates in box_coordinates_r.txt

Modified coordinates as a square box in stage2/box_coordinates_s.txt
